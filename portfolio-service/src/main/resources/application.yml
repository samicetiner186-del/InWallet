server:
  port: 8080

jwt:
  secret: InWallet2026HackathonSecretKeyForJWTMustBe256BitsLong
  expiration: 86400000      # 24 saat (ms) - Hackathon boyunca sorunsuz çalışma için uzatıldı
  refreshExpiration: 604800000 # 7 gün (ms)

spring:
  application:
    name: portfolio-service
  datasource:
    url: jdbc:postgresql://postgres:5432/wallet_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: redis
      port: 6379

  cache:
    type: redis
    redis:
      time-to-live: 600000

  kafka:
    bootstrap-servers: kafka:29092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: portfolio-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    com.wallet.portfolio: DEBUG

# ─── Resilience4j — Circuit Breaker & Retry ──────────────────────────────────
resilience4j:
  circuitbreaker:
    instances:
      yahooFinance:
        # 5 başarısız istek → devre açılır
        slidingWindowSize: 5
        failureRateThreshold: 60
        # 30 sn açık kaldıktan sonra "half-open" duruma geçer
        waitDurationInOpenState: 30s
        permittedNumberOfCallsInHalfOpenState: 2
        registerHealthIndicator: true
      geminiApi:
        slidingWindowSize: 3
        failureRateThreshold: 67
        waitDurationInOpenState: 20s
        permittedNumberOfCallsInHalfOpenState: 1
        registerHealthIndicator: true
  retry:
    instances:
      yahooFinance:
        maxAttempts: 3
        waitDuration: 1s
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2
      geminiApi:
        maxAttempts: 2
        waitDuration: 500ms
